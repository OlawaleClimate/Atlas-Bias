name: Zenodo Release

on:
  release:
    types: [published]

jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: |
          echo "Integrate Zenodo deposition API here."
          echo "Release tag: ${{ github.ref_name }}"
